import { createTheme } from "@mui/material/styles";

export const darkTheme: any = createTheme({
    palette: {
        mode: "light",
        // white: {
        //     main: '#64748B',
        // }
    },
});